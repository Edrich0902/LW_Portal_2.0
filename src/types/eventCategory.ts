export enum EventCategory {
    GENERAL = 'general', // all general events like cell groups and weekly events
    ONCEOFF = 'onceoff', // once off special events like Vroue oggend
    COURSE = 'course', // all courses are given a specific category of course
}